// Excersise 2: compare two values

// Write a function compare which returns the boolean result of 
// its two parameters by checking that they have equal value and equal type.

function compare(x,y) {
    return x === y;
}